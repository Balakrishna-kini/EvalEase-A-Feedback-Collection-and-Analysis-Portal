spring.application.name=evalease-backend

# Use environment variables for deployment, fallback to local MySQL for development
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/formcollectionn}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:balu2004}
spring.datasource.driver-class-name=${DB_DRIVER:com.mysql.cj.jdbc.Driver}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=${DB_DIALECT:org.hibernate.dialect.MySQLDialect}
server.port=${PORT:8080}

# JWT Secret (In production, use a strong key from env)
jwt.secret=${JWT_SECRET:9a4f2c8d3b7a1e5f2c8d3b7a1e5f2c8d3b7a1e5f2c8d3b7a1e5f2c8d3b7a1e5f}
jwt.expiration=86400000